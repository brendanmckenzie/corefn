version: '2'
services:
  redis:
    image: redis
  host:
    build: host
    ports:
      - 8080:8080
    volumes:
      - /home/corefn/manifest:/var/func/manifest
    links:
      - redis:redis
  cleaner:
    build: cleaner
    links:
      - redis:redis
