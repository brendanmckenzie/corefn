version: '2'
services:
  redis:
    image: redis
  host:
    build: host
    ports:
      - 8080:8080
    volumes:
      - /Users/brendan/Projects/corefn/docker/host/corefn/manifest:/var/func/manifest
      - /var/run/docker.sock:/var/run/docker.sock
    depends_on:
      - redis
    links:
      - redis:redis
  cleaner:
    build: cleaner
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
    depends_on:
      - redis
    links:
      - redis:redis

